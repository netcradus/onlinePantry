{
  "name": "online-pantry-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix server",
    "build": "echo 'Root build complete'",
    "start": "npm start --prefix server"
  }
}
